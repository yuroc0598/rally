"""Decision layer: confidence-weighted fusion of the signal channels
(:mod:`score`), temporal decoding of the probability into rally regions
(:mod:`decode`), tight point derivation from ball strikes (:mod:`points`), and
ball-based point-end refinement (:mod:`ball_end`)."""
