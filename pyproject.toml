[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rally"
version = "0.1.0"
description = "Trim a tennis match recording down to rally (live-play) segments only."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "imageio-ffmpeg>=0.4",
]

[project.optional-dependencies]
headless = ["opencv-python-headless>=4.8"]
players = ["opencv-python>=4.8", "ultralytics>=8.3.78"]
pose = ["rtmlib>=0.0.15", "onnxruntime>=1.17"]
ball = ["torch>=2.0", "gdown>=5.0"]
web = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-multipart>=0.0.9",
]
dev = ["pytest>=7.0", "httpx>=0.27"]
training = ["scikit-learn>=1.3", "joblib>=1.3"]
server = [
    "opencv-python-headless>=4.8",
    "ultralytics>=8.3.78",
    "rtmlib>=0.0.15",
    "onnxruntime>=1.17",
    "torch>=2.0",
    "gdown>=5.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
]
desktop = [
    "opencv-python>=4.8",
    "ultralytics>=8.3.78",
    "rtmlib>=0.0.15",
    "onnxruntime>=1.17",
    "torch>=2.0",
    "gdown>=5.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
]
# Backward-compatible alias for the headless/server feature set.
all = [
    "opencv-python-headless>=4.8",
    "ultralytics>=8.3.78",
    "rtmlib>=0.0.15",
    "onnxruntime>=1.17",
    "torch>=2.0",
    "gdown>=5.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
]

[project.scripts]
rally = "rally.cli:main"
rally-web = "rally.web.app:main"
rally-evaluate = "rally.tools.evaluate:main"

[tool.setuptools.packages.find]
include = ["rally*"]

[tool.setuptools.package-data]
"rally.web" = ["static/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
