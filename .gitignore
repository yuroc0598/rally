.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
tests/_scratch/
tests/_scratch_web/

# web UI runtime data (uploads, outputs, job metadata)
.rally_web/
_demo/
*.mp4
!tests/fixtures/*.mp4
*.pt
candidates/
train_candidates/
*.h5
models/*.h5

# environment-specific ffmpeg shim symlinks (point into .venv)
.ffbin/

# generated run outputs (JSON sidecars, logs, batch script; .mp4 already ignored)
samples/

# internal AI review / audit notes (not part of the deliverable)
feedback.claude
feedback.muse

# Local Claude Code session/agent state
.claude/
