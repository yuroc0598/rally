.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
tests/_scratch/
tests/_scratch_web/

# Visible runtime session data (uploads, outputs, job metadata, golden results)
sessions/

# Legacy runtime locations retained in ignore rules for older installations
.rally_web/
.rally_golden*/
_demo/
*.mp4
!tests/fixtures/*.mp4
*.pt
candidates/
train_candidates/
*.h5

# local model binaries; retain only the directory documentation
models/*
!models/README.md
!models/*.manifest.json

# environment-specific ffmpeg shim symlinks (point into .venv)
.ffbin/

# local golden inputs, annotations, and generated evaluation outputs
samples/*
!samples/README.md

# internal AI review / audit notes (not part of the deliverable)
feedback.codex
feedback.claude
feedback.muse
feedback.performance
MODEL_PROVENANCE.md

# Local Claude Code session/agent state
.claude/
